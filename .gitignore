
.DS_Store
.zed
.agents
.claude
.fallow

node_modules

# raw compiled binaries (scripts/build.ts); never committed
dist/bin

# staged in from dist/bin/ by scripts/publish.ts right before `npm publish`,
# removed again immediately after; never meant to be committed
dist/outrider-*/bin/outrider
